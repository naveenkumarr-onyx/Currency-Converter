import React, { useEffect, useState } from "react";

const useCurrencyInfo = () => {
  const [data, setData] = useState({});
  useEffect(() => {
    fetch(``);
  }, []);
  return <div>useCurrencyInfo</div>;
};

export default useCurrencyInfo;
